stages:
  - build
  - release


build:
  stage: build
  script:
    - docker-compose build

release:
  stage: release
  script:
    - docker-compose -f docker-compose.yml -f prod.certbot.yml up -d
    - docker-compose exec -T app python manage.py collectstatic -h

  only:
    - gitlab-ci
